import 'package:flutter/material.dart';
import 'package:nyari_1/theme.dart';

class CategoryPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is Category Page', style: whiteTextStyle),
      ),
    );
  }
}
